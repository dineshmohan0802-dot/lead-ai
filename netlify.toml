[build]
  command = "npm run build"
  functions = "dist"
  publish = "dist"

# SPA fallback for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  conditions = { Role = ["anonymous", "authenticated"] }

